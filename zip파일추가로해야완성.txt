
----- GameSystem 의 멤버변수를 초기화하는 초기화 함수 Init 은 패킷이 왔을 때 처리되지만 GameSysten자체를 초기화하는 Init()은 따로 정의해서 호출해줘야함.
(또한 Init 초기화하면서 StaticLogicProxy를 통해 워커스레드 생성해야함 -> LobbySystem 참고)

GameLogic() 함수 마저 수정하기



★ 현재 각 서버는 모든 세션에 대해서 패킷 받으면 처리하지만, 내 User가 아닌 녀석에게 온 패킷을 걸러내는 작업이 필요 (Guest 등 유저 전과 후의 패킷을 처리할 수 있는 과정이 필요함)

1. GameServer는 틱 단위로 로직을 처리하도록 변경한다.





6. GameLanClient 연결되면 바로 요청패킷보내도록해야함. (내 예상으로는 아마도 OnConnected 지점에서 호출해줘야할듯')
\
7. 게임 서버의 경우 디버깅 아닌 실행파일 형태로 실행되면 파일 위치를 찾지 못한다. (절대 경로, 상대 경로로 설정 해도 모두 인식을 못함)
8. 메인 -> 보조 서버/클라이언트의 Stop / Start가 제대로 실행되는지 확인한다.

9. GameWorld의 CreateGamePlayer 수정 후 다시 테스트..




----

1. Player의 SendPositionUpdate 함수

	//if ((currentPos - m_lastUpdatePos).sqrMagnitude() < 0.01f)
	//	return;
이 부분은 원래는 주석 해제였지만 현재는 Position Update가 Move에서만 이루어지고 있고, Move 다음에 SendPositionUpdate가 호출되므로 주석처리를 하고 테스트를 한다,.

기본적인 동작은 하게 했는데

2. 메모리 사용량에 대한 로깅 / 프린트, 더미 테스트도 해봐야한다.
3. 더미 클라이언트를 어떻게 만들 것인가에 대한 생각..
